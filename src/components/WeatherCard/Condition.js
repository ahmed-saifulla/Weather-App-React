import React from "react";
import styled from '@emotion/styled';

const Condition = (props) => {


  const ConditionText = styled.h3`
`

  return (
    <ConditionText > Cloudy </ConditionText>
  );
};

export default Condition;
