import React from "react";
import styled from '@emotion/styled';


const Icon = (props) => {



  const Image = styled.img`
  `

  return (
    <Image src="./img/clouds.webp" alt="Weather Icon" />
  );
};

export default Icon;
